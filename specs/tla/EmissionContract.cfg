\* TLC configuration for EmissionContract.
\* One source delta and a single restart are enough to expose the
\* effective-once violation (outbox resurrection across an epoch bump).
SPECIFICATION Spec

CONSTANTS
    NumDeltas = 1
    MaxEpoch  = 2

INVARIANT TypeOK
INVARIANT EffectiveOnce
