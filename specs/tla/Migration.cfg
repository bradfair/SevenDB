\* Real code behavior: outbox keyed/ordered by commit index only.
\* NoLoss is EXPECTED TO FAIL (overwrite loss + epoch-regression stranding).
SPECIFICATION Spec
CONSTANTS
    MaxDeltas        = 3
    EpochAwareOutbox = FALSE
INVARIANT TypeOK
INVARIANT NoLoss
