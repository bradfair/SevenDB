\* TLC configuration for the Reconnect (P5) spec.
\* ServerEpoch is any nonzero value (the live wall-clock epoch). MaxIdx small.
\* ReconnectSound is EXPECTED TO FAIL (it documents the bug, with a witness).
\* To confirm the property is not vacuous, check FixHolds instead (expected PASS):
\*   ... -invariant FixHolds  (or swap the INVARIANT line below).
SPECIFICATION Spec

CONSTANTS
    MaxIdx      = 3
    ServerEpoch = 7

INVARIANT ReconnectSound
